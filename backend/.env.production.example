# ---- PRODUCTION (sample/placeholder - DO NOT commit real secrets) ----
PROJECT_NAME="Clean Water Delivery Management System"
API_V1_PREFIX="/api/v1"
DEBUG=False

# Supabase Credentials (CRITICAL - replace with production values)
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_KEY="your-production-service-role-key"
SUPABASE_JWT_SECRET="your-production-jwt-secret"

# CORS: explicit allow-list (no wildcard). Uses CORS_ORIGINS_JSON (validated alias).
CORS_ORIGINS_JSON=["https://admin.yourcompany.com", "https://yourcompany.com", "capacitor://localhost"]

# Sentry (Optional)
SENTRY_DSN="https://your-sentry-dsn@ingest.sentry.io/0000000"

# SMS & Notifications (Optional)
SMS_BASE_URL="https://api.sms-provider.com"
SMS_API_KEY="your-api-key"
SMS_SENDER="your-sender-id"
TWILIO_ACCOUNT_SID="ACxxx"
TWILIO_AUTH_TOKEN="your-token"

# Redis (Optional, for persistent rate limiting across instances). If unreachable
# the app gracefully falls back to in-memory storage (with a startup warning).
REDIS_URL="redis://your-redis-host:6379/0"

# Database connection strings
# DATABASE_URL should point to the pooled pgBouncer URL (port 6543) for runtime
# usage by the application. DIRECT_URL should point to the direct DB URL (port 5432)
# for migrations and administrative operations.
DATABASE_URL="postgresql://postgres:PYDvzNeBdnhKlHYu@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:PYDvzNeBdnhKlHYu@db.eettoiqgeecfnjbbuyni.supabase.co:5432/postgres"
