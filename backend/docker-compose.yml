version: '3.8'

services:
  backend:
    build: .
    container_name: clean_delivery_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Optional: Redis for Rate Limiting or Caching
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
